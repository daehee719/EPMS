# 📌 EPMS(EduProgram Management System)

## 행정 프로그램·교육과정 관리 시스템

본 프로젝트는 전자정부 표준프레임워크(eGovFrame) 4.3 기반으로  
**행정 데이터(교육과정/프로그램) 관리 및 엑셀 업로드 기능**을 구현하고,  
이를 **Spring Boot 및 eGovFrame 5.0 라인**,  
나아가 **프론트엔드 분리(Next.js)** 구조로 단계적으로 전환하는 학습 프로젝트입니다.

---

## 목차
- [1. 프로젝트 목적](#1-프로젝트-목적)
- [2. 프로젝트 범위](#2-프로젝트-범위)
- [3. 기술 스택](#3-기술-스택)
- [4. 데이터 모델 개요](#4-데이터-모델-개요)
- [5. 엑셀 업로드 정책](#5-엑셀-업로드-정책)
- [6. 프로젝트 구조 (초기)](#6-프로젝트-구조-초기)
- [7. MariaDB 사용 정책](#7-mariadb-사용-정책)
- [8. 프론트엔드 전환 계획](#8-프론트엔드-전환-계획)
- [9. 마이그레이션 전략](#9-마이그레이션-전략)
- [10. 향후 계획](#10-향후-계획)
- [11. 참고](#11-참고)
- [12. 로컬 환경 설정(클론 받은 사람용)](#12-로컬-환경-설정클론-받은-사람용)
- [13. DB 초기화 스크립트](#13-db-초기화-스크립트)

---

## 1. 프로젝트 목적
공공 SI 프로젝트에서 반복적으로 요구되는  
**대량 행정 데이터 관리 + 엑셀 업로드/검증/이력 관리** 기능을  
전자정부 표준프레임워크 방식으로 구현하고,

이후
- Spring Boot 기반 백엔드 구조 전환
- JDK 17 / Jakarta EE 기반(eGovFrame 5.0 라인) 마이그레이션
- 프론트엔드 React(Next.js) 기반 분리

까지의 **전환 과정을 단계적으로 경험**하는 것을 목표로 합니다.



EduAdmin은 공공·교육기관에서 운영하는
교육과정 및 프로그램 정보를 하나의 기준 데이터로 관리하기 위한
행정 지원 서비스입니다.

분산되어 관리되던 교육과정 정보를 체계적으로 정리하고,
엑셀 업로드를 통해 대량 등록·수정 작업을 효율적으로 수행할 수 있도록 지원합니다.
업로드 과정에서 데이터 검증과 오류 안내를 제공하여
잘못된 정보가 시스템에 반영되는 것을 사전에 방지합니다.

또한, 변경 이력과 업로드 결과를 관리하여
교육과정 데이터 관리 과정의 투명성과 신뢰성을 확보하는 것을 목표로 합니다.

---

## 2. 프로젝트 범위
### ✅ 구현 대상 데이터
- 교육과정 / 프로그램 마스터 데이터

### ✅ 주요 기능
- 프로그램 목록 조회 (검색 / 페이징)
- 프로그램 단건 등록·수정·삭제 (CRUD)
- 엑셀 템플릿 다운로드
- 엑셀 업로드를 통한 대량 등록/수정 (UPSERT)
- 업로드 검증 및 오류 행 단위 피드백
- 업로드 이력 관리

---

## 3. 기술 스택
### (1) 초기 구현 환경 – eGovFrame 4.3
| 구분 | 내용               |
|---|------------------|
| Framework | 전자정부 표준프레임워크 4.3 |
| Language | Java 8           |
| Build Tool | Maven 3.6.3      |
| WAS | Tomcat 8.5.x     |
| ORM | MyBatis          |
| DB | MariaDB          |
| IDE | IntelliJ         |

- XML 기반 설정
- WAR 배포 + 외부 Tomcat 실행 방식
- JSP + jQuery 기반 화면 구성

---

### (2) 마이그레이션 목표
| 단계 | 목표 |
|---|---|
| Step 1 | Spring Boot 기반 프로젝트로 구조 전환 |
| Step 2 | 설정 파일 XML → Java Config / YAML |
| Step 3 | JDK 17 + Spring Boot 3 |
| Step 4 | Jakarta EE 기반(eGovFrame 5.0 호환) |
| Step 5 | 프론트엔드 Next.js 기반 분리 |

---

## 4. 데이터 모델 개요
### 핵심 키 정책
- `program_code`를 기준으로 **UPSERT 처리**
    - 존재 시 → UPDATE
    - 미존재 시 → INSERT

### 주요 테이블
- `adm_program` : 교육과정/프로그램 마스터
- `adm_upload_job` : 엑셀 업로드 작업 이력
- `adm_upload_error` : 업로드 오류 상세
- `com_code` / `com_code_detail` : 공통코드 관리

### adm_program 테이블 구조
| 컬럼 | 타입 | 설명 |
|---|---|---|
| `PROGRAM_CODE` | VARCHAR(30) | 프로그램 코드 (PK) |
| `PROGRAM_NAME` | VARCHAR(200) | 프로그램 명칭 |
| `START_DATE` | DATE | 시작일 |
| `END_DATE` | DATE | 종료일 |
| `CAPACITY` | NUMERIC(10) | 정원 |
| `USE_YN` | CHAR(1) | 사용 여부 (Y/N) |
| `DESCRIPTION` | VARCHAR(1000) | 설명 |
| `REG_ID` | VARCHAR(20) | 등록자 ID |
| `REG_DT` | DATETIME | 등록일시 |
| `UPD_ID` | VARCHAR(20) | 수정자 ID |
| `UPD_DT` | DATETIME | 수정일시 |

---

## 5. 엑셀 업로드 정책
- 업로드 방식: **UPSERT**
- 검증 단계
    1. 필수값 / 형식 검증
    2. 공통코드 유효성 검증
    3. 비즈니스 룰 검증 (기간, 정원 등)
- 오류 발생 시
    - 전체 롤백
    - 오류 행 단위 상세 리포트 제공

---

## 6. 프로젝트 구조 (현재)
```text
src
 └─ main
    ├─ java
    │   └─ egovframework/com/edu/prm
    │       ├─ web         (Controller)
    │       ├─ service     (Service)
    │       └─ service/impl
    ├─ resources
    │   └─ egovframework/mapper/com/edu/prm
    └─ webapp/WEB-INF/jsp/egovframework/com/edu/prm
```
---

## 7. MariaDB 사용 정책
- Character Set: `utf8mb4`
- Collation: `utf8mb4_general_ci`
- 날짜/시간 타입
    - `DATE` : 시작일 / 종료일
    - `DATETIME` : 생성 / 수정 일시
- 대량 업로드 고려사항
    - 트랜잭션 범위 최소화
    - 배치 INSERT / UPDATE 고려
    - 인덱스는 업로드 완료 후 적용 검토

---

## 8. 프론트엔드 전환 계획
초기 구현은 eGovFrame 4.3 환경에서  
**JSP + jQuery 기반 서버 렌더링 화면**으로 진행하며,

이후 프론트엔드를 **React 기반 Next.js**로 전환하여  
**백엔드(API)와 프론트엔드 분리 구조**로 마이그레이션합니다.

### Legacy Frontend
- JSP
- jQuery

### Target Frontend
- Next.js **16.1**
- React **19.2**
- Node.js **24 (LTS)**

> 기존 화면은 단계적으로 API 호출 방식으로 전환하며,  
> 최종적으로는 프론트엔드 독립 배포 구조를 목표로 합니다.

---

## 9. 마이그레이션 전략
본 프로젝트는 **기능 구현보다 “전환 과정” 자체를 핵심 학습 포인트**로 둡니다.

- eGovFrame 4.3
    - XML 기반 설정
    - WAR 배포 + 외부 Tomcat 실행
- Spring Boot 전환
    - 내장 Tomcat 기반 실행
    - 설정 파일 단순화 (YAML)
- Boot 3 / JDK 17 전환
    - `javax → jakarta` 네임스페이스 대응
    - 라이브러리 호환성 문제 해결
- Next.js 전환
    - JSP/jQuery 제거
    - REST API 기반 프론트엔드 분리

---

## 10. 향후 계획
- [ ] 엑셀 업로드 오류 다운로드 기능
- [ ] 대량 업로드 성능 개선
- [ ] Spring Boot 기반 REST API 고도화
- [ ] eGovFrame 5.0 환경 완전 전환
- [ ] Next.js 기반 관리자 UI 완성

---

## 11. 참고
- 전자정부 표준프레임워크 공식 문서
- Spring Boot Migration Guide
- 공공 SI 프로젝트 개발 관행 기반 설계

---

## 12. 로컬 환경 설정(클론 받은 사람용)
이 프로젝트는 **Maven 프로필(dev/prod)**에 따라  
`src/main/resources-dev` 또는 `src/main/resources-prod`의 설정이 적용됩니다.

> 주의: 공개 저장소에서 사용하는 값은 **개발용 기본 비밀번호**만 사용하세요.  
> 운영/실데이터 비밀번호는 절대 커밋하지 마세요.

### 기본값(DEV)
파일: `src/main/resources-dev/egovframework/egovProps/globals.properties`
```properties
Globals.DbType = maria
Globals.maria.DriverClassName=org.mariadb.jdbc.Driver
Globals.maria.Url=jdbc:mariadb://127.0.0.1:3306/epms_dev
Globals.maria.UserName = epms_dev_user
Globals.maria.Password = vjxt4bc0wA-bCyXWlSvqLw
```

### 운영 예시
파일: `src/main/resources-prod/egovframework/egovProps/globals.properties`
```properties
Globals.DbType = maria
Globals.maria.DriverClassName=org.mariadb.jdbc.Driver
Globals.maria.Url=jdbc:mariadb://127.0.0.1:3306/epms_prod
Globals.maria.UserName = epms_prod_user
Globals.maria.Password = mcLM3UZFTYZf6KFIyF2uLg
```

---

## 13. DB 초기화 스크립트
클론한 사람이 **DB/유저/초기데이터를 한 번에 설정**할 수 있도록 스크립트를 제공합니다.

### macOS/Linux
```sh
chmod +x scripts/db/setup-db.sh
./scripts/db/setup-db.sh
```

### Windows
```bat
scripts\\db\\setup-db.bat
```

스크립트가 수행하는 작업:
- epms_dev / epms_prod DB 생성
- epms_dev_user / epms_prod_user 생성 및 권한 부여
- (선택) DDL/DML 적용

기본 비밀번호(로컬 공통):
- DEV: `epms_dev_pass1!`
- PROD: `epms_prod_pass1!`

Test CI trigger: 2026-02-03 11:22 KST
